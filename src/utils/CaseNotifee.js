
import notifee, { EventType } from '@notifee/react-native';

export default async function CaseNotifee(type, detail) {
  switch (type) {
    case EventType.DISMISSED:
      break;
    case EventType.PRESS:
      break;
  }
}
