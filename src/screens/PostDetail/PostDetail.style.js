import { Image } from "react-native";
import styled from "styled-components";

export const ImagePostDetail = styled(Image)`
    width:100%;
    height:300px;
    resize-mode:cover;
    border-radius:5px;
`;